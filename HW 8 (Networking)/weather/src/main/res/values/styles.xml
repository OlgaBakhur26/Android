<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="homeAsUpIndicator">@drawable/ic_home</item>
    </style>

    <style name="itemWeatherTextStyle">
        <item name="fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/item_text_size</item>
        <item name="android:textColor">@color/itemTextColor</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="mainViewTextStyle">
        <item name="fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/main_view_text_size_Big</item>
        <item name="android:textColor">@color/itemTextColor</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="cityItemTextStyle">
        <item name="fontFamily">sans-serif</item>
        <item name="android:textSize">@dimen/cityListTextSize</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:gravity">left|center_horizontal</item>
    </style>

</resources>