<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbarSettings"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/toolbarColor">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="@string/toolbarSettingsTitle"
                android:gravity="center_vertical|left"
                android:textSize="@dimen/toolbarTextSize"
                android:fontFamily="sans-serif-medium"
                android:textColor="@android:color/white"/>
        </LinearLayout>
    </androidx.appcompat.widget.Toolbar>

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/framePadding">

        <RadioButton
            android:id="@+id/viewRadioCelsius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/unitCelsius"
            android:textSize="@dimen/settings_text_size"
            android:textColor="@android:color/black"
            android:checked="true"
            android:layout_marginBottom="@dimen/views_inside_margin"/>

        <RadioButton
            android:id="@+id/viewRadioFahrenheit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/unitFahrenheit"
            android:textSize="@dimen/settings_text_size"
            android:textColor="@android:color/black"/>

    </RadioGroup>

</LinearLayout>